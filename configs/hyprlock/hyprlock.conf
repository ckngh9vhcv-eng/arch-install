background {
    monitor =
    path = ~/wallpapers/wallhaven-49z1pw_2560x1440.png
    blur_passes = 4
    blur_size = 5
    brightness = 0.85
    contrast = 0.95
}

# Clock
label {
    monitor =
    text = cmd[update:1000] echo "$(date +%H:%M)"
    font_size = 80
    font_family = FiraCode Nerd Font
    color = rgba(224, 212, 240, 1.0)
    position = 0, 200
    halign = center
    valign = center
}

# Date
label {
    monitor =
    text = cmd[update:60000] echo "$(date '+%A, %B %d')"
    font_size = 18
    font_family = FiraCode Nerd Font
    color = rgba(184, 160, 214, 1.0)
    position = 0, 130
    halign = center
    valign = center
}

# Password input
input-field {
    monitor =
    size = 300, 50
    outline_thickness = 2
    dots_size = 0.25
    dots_spacing = 0.3
    dots_center = true
    dots_rounding = -1
    outer_color = rgba(139, 111, 192, 0.6)
    inner_color = rgba(16, 16, 30, 0.85)
    font_color = rgba(224, 212, 240, 1.0)
    fade_on_empty = true
    placeholder_text = <i>Enter password...</i>
    hide_input = false
    rounding = 12
    check_color = rgba(139, 111, 192, 1.0)
    fail_color = rgba(204, 34, 34, 0.8)
    fail_text = <i>Authentication failed</i>
    position = 0, -20
    halign = center
    valign = center
}
